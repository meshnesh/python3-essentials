# regular expression are very powerful method of matching patterns in text
# can be used for simple search an d replsce to complex data manupilation

# they are implemented in python by the "re" module

# they are called in the format

import re

pattern =re.compile(r'\d\d\d')
	if re.search(regex, line): print(line)